cmake_minimum_required(VERSION 2.8)
project(enum-utils CXX)
set(tgt enum-utils-test)
add_executable(${tgt} test.cpp enum.h)
target_include_directories(${tgt} PUBLIC ".")
set_property(TARGET ${tgt} PROPERTY CXX_STANDARD 11)
